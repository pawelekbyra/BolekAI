NEXT_PUBLIC_BOLEK_API_URL=https://agent-bolek.<twoj-subdomain>.workers.dev
# Must match the Worker's BOLEK_API_KEY secret (wrangler secret put BOLEK_API_KEY).
# NOTE: this ships inside the browser bundle - see "Corrections" in docs/SYSTEM.md.
NEXT_PUBLIC_BOLEK_API_KEY=
